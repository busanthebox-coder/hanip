# 지시 32 — 라이브 결함 카드 33장 + 신규 챕터 두께 + 위생

## 1. 라이브 결함 카드 33장 (최우선)
`docs/harness/audit/snack-cards.json`의 `preexisting.findings` — 지시 26이 발견하고 범위 밖이라
기록만 한 것들. **지금 라이브에 있고 학습 가치가 0이다**:
- `snack-numbers` **23장 전부 선택지 1개** — 「하나」를 만나면 고를 게 하나뿐이라 틀릴 수가 없다
- `snack-reactions-*` 10장 선택지 2개

원인은 오답 생성기의 `sharesContentWord`가 "one (native number)" 같은 글로스에서 같은 내용어를
공유한다고 판단해 후보 풀을 전멸시키는 것(지시 25 보고). **테스트 선행으로 고쳐라**:
- 숫자·맞장구처럼 글로스가 체계적으로 겹치는 팩에서도 **선택지 3개**가 나와야 한다
- 오답이 정답과 의미가 겹치면 안 된다(하나 vs one/first 같은 동의 오답 금지)
- 기존 정상 카드의 선택지는 바뀌어도 되지만, **바뀐 카드 수와 이유를 보고**하라

## 2. 신규 챕터 66~72 두께
3단계 생성기(지시 21~22)가 이 문법 유형을 만난 적이 없어 신규 7과만 기준 미달이다:
- **문법 문제 <2인 바이트 7개**: `chapter-68-b4` `chapter-70-b8` `chapter-71-b2` `chapter-71-b6`
  `chapter-72-b2` `chapter-72-b4` `chapter-72-b8` (전부 q=1)
- **1장 바이트 1개**: `chapter-67-b10`(읽기)
- 목표: 나머지 65과와 같은 기준 — 문법 바이트 문제 ≥2, 1장 바이트 0
- 지시 21~22의 **생성기를 확장**하되(관용구·피동 접미사·문어 연결어미는 제목에서 형태소 추출이
  안 된다 — 다른 근거를 써라: formTable 행, examples, 대화·지문의 실제 출현), **새 한국어 저작 금지**.
  기존 65과의 카드가 바뀌면 안 된다(카드키 전수 대조로 증명).

## 3. 위생
`.claude/worktrees/` 아래 다른 세션의 워크트리 잔재가 있으면 **vitest 이중 수집**을 유발한다.
`git worktree list`로 확인하고, **본 트리에 등록되지 않은 고아 디렉터리만** 정리하라.
살아 있는 워크트리는 절대 지우지 마라 — 판단이 안 서면 건드리지 말고 보고.

## 게이트
- 1·2번은 **컴파일 산출이 바뀐다**(라이브 카드 변경). 따라서:
  - `node scripts/audit-generated.mjs` — 신규·변경 카드 blocker 0
  - `node scripts/report-density.mjs after-32` — 문법 문제 ≥2 **100%**, 1장 바이트 **0**, 중간값 보고
  - **기존 65과 카드 무변경**을 카드키 대조로 증명(신규 챕터와 간식 팩만 변경 허용)
  - 재스냅샷은 **감독자 승인 사항**이다. 스냅샷을 갱신하지 말고, 무엇이 바뀌었는지만 보고하라.
    `baseline.mjs --write` 실행 금지.
- `npm run guard:full`은 baseline 불일치로 레드가 정상이다. 대신 `npm run guard:thin`(baseline 생략)
  으로 테스트·검증·예산을 확인하고, baseline 차이의 내역을 보고하라.
- dev QA: 숫자 팩 3장(선택지 3개 확인) · 맞장구 팩 1장 · 68·71·72과 문법 바이트 각 1개 · 67과 읽기.

## 마감
PROGRESS 32행, 커밋 1개 `[order-32] fix unmissable pack cards and thicken chapters 66-72`.
**푸시·배포 금지**(감독자 몫).

## 완료 기준
결함 33장 해소(선택지 ≥3) · 문법 문제 ≥2 100% · 1장 바이트 0 · 기존 65과 카드 무변경 증명 ·
blocker 0 · guard:thin 그린 · baseline 차이 내역 보고(재스냅샷은 감독자가)
