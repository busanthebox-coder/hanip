# 데이터 사실 교정 기록

원본 챕터 JSON의 사실 오류를 감사 프로토콜에 따라 교정한 기록이다.

| 날짜 | 파일 | 변경 전 | 변경 후 | 사유 | 부모 저장소 |
|---|---|---|---|---|---|
| 2026-08-01 | `data/chapters/chapter-13.json` | `뵈었어요 → 봬어요` | `뵈어요 → 봬요, 뵈었어요 → 뵀어요` | 현재형·과거형 축약을 섞은 잘못된 활용 설명 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-13.json` | 읽기에 없는 `이해할 수 있다고`를 정답으로 제시 | 실제 본문의 `봐야 한다고`와 결합 설명 제시 | 읽기 본문과 QA 정답 불일치 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-14.json` | `지다 → 질까 봐: ㄹ irregular` | 규칙적인 모음 끝 어간이 `ㄹ까 봐`를 취한다고 설명 | `지다`를 ㄹ 불규칙으로 잘못 분류한 설명 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-16.json` | 동사 예시 `늦다 → 늦는 바람에` | `놓치다 → 놓치는 바람에` | 바로 다음 행의 “형용사에는 사용하지 않음”과 충돌하지 않는 명확한 동사 예시로 교체 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-17.json` | 본문에 없는 `왔어요`를 축약 과거형 정답으로 제시 | 실제 본문의 `만났어요`, `갔고`, `봤어요` 제시 | 읽기 본문과 QA 정답 불일치 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-20.json` | `몇 시 십 분에 만나기로 했어요?` | `몇 시 몇 분에 만나기로 했어요?` | 분 값을 묻지 않고 십 분을 미리 제시한 질문 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-23.json` | 자연스러운 `이 길을 쭉 가세요`를 오답 처리 | 비문 `이 길이 쭉 가세요`로 오답 교체, `을`의 이동 경로 표지 가능성 명시 | 복수 정답 드릴 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-24.json` | `입어 보세요라고 말했어요` | `입어 보라고 말했어요` | 따옴표 없는 간접 인용에서 청유형 종결어미에 `라고`를 직접 결합한 오류 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-26.json` | 따옴표 없이 `오나 봐요라고`, `몰라요라고` 결합 | 발화 전체를 명시적으로 인용하고 `라고` 결합 | 직접 인용 표기 오류 교정 | 동일 오류 있음 |
| 2026-08-01 | `data/chapters/chapter-26.json` | 본문에 없는 ‘밖에서 기다리기 불편해서’를 입장 이유로 제시 | 수진 씨의 상황이 확실하지 않았다는 본문 근거로 교체 | 읽기 본문과 QA 정답 불일치 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-27.json` | `벌써`는 미래 표현과 결합할 수 없다고 단정 | 완료·현재 상태뿐 아니라 놀람을 나타내는 임박 계획 질문 `벌써 갈 거예요?`도 가능하다고 설명 | 정상 용법을 금지하는 어휘 설명 교정 | 부모 어휘 원본에 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-27.json` | `계속`은 반드시 중단 없이 이어지고 `자꾸`에만 간격이 있다고 단정 | `계속`은 기간에 걸친 지속, `자꾸`는 반복과 흔한 불만 뉘앙스를 부각한다고 설명 | 실제 반복 용법을 배제하는 절대 규칙 교정 | 부모 어휘 원본에 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-27.json` | `-는 중이에요`는 지속 상태에 쓸 수 없고 `사는 중이에요`는 불가능하다고 설명 | `살고 있어요`가 중립적 기본형이지만 현재 단계로 제시할 때 `사는 중이에요`도 가능하다고 설명 | 가능한 상태 동사 용법을 금지한 설명 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-27.json` | `-는 동안`의 두 사건이 같은 전체 기간을 차지해야 한다고 설명 | 주절 사건은 제시된 시간 구간 전체 또는 그 안의 한 시점에 일어날 수 있다고 설명 | 필수적인 완전 동시성으로 잘못 제한한 설명 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-29.json` | `V-다가`의 두 절은 항상 같은 사람이 한다고 단정 | 의도적 행위는 동일 주어가 보통이나 외부 사건은 다른 주어가 될 수 있다고 설명 | `밥을 먹다가 전화가 왔어요`와 충돌하는 절대 규칙 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-29.json` | 완료된 순간 동작 `문자를 받다가`를 중단된 진행 동작으로 설명 | 진행 중 중단될 수 있는 `문자를 읽다가`로 예문·번역·설명 교체 | `-다가`의 중단 의미를 잘못 가르치는 드릴 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-30.json` | `고장나다`, `버스가 고장나다`, `핸드폰이 고장나다` | `고장 나다`, `버스가 고장 나다`, `핸드폰이 고장 나다` | `고장이 나다`에서 조사 생략 시 적용되는 표준 띄어쓰기 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-30.json` | 약속에 늦는 뜻의 `늦다`를 형용사로 두고 `답장이 늦다`와 혼합 | 품사를 동사로 바꾸고 같은 뜻의 `수업에 늦다` 및 동사 의미 설명 사용 | 동음 표제어의 품사·의미 혼합 교정 | 부모 챕터·어휘 원본에 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-30.json` | `늦는 [난는]` | `늦는 [는는]` | 잘못된 비음화 결과 발음 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-31.json` | 모음 끝 명사 행에 `가족 → 가족을 위해서` | `친구 → 친구를 위해서` | 모음 끝 명사와 `를` 결합을 실제로 보여 주도록 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-31.json` | `머물러요`를 `머물-`의 ㄹ 중첩으로 설명 | `머물다`는 `머무르다`의 준말이며 모음 시작 활용은 `머무르다`의 르 불규칙에서 나온다고 설명 | 표면형은 맞지만 형태 분석이 틀린 설명 교정 | 부모 어휘 원본에 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-31.json` | `일주일 동안`의 필수 발음을 ㄹ 연음에 의한 `[일쭈일 똥안]`으로 설명 | 주의 깊은 발음 `[일쭈일 동안]`, 선택적 된소리 `[똥안]`으로 구분 | 단어 경계의 선택적 된소리를 필수 연음으로 오인한 설명 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-31.json` | 동일 주어 행에 `밥을 먹는 동안 전화가 왔어요` | `밥을 먹는 동안 텔레비전을 봤어요` | 먹는 사람과 전화의 주어가 다른 예시를 동일 주어로 분류한 오류 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-32.json` | `이사하다`와 `옮기다`, `안 이사해요`에 관한 절대 금지 | 거주지 이동과 물건 이동의 차이, `집을 옮기다`, 두 부정 어순의 가능 범위를 구분 | 가능한 `옮기다` 용법과 부정 어순을 금지한 어휘 설명 교정 | 부모 어휘 원본에 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-32.json` | `많다 → 많아졌어요`를 기타 모음 행에 배치 | ㅏ/ㅗ 행으로 이동 | 표의 분류와 자체 설명이 충돌하는 오류 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-32.json` | `시작하기로` 로마자 `sijakagiro` | `sijakhagiro` | `하` 음절의 `h`가 빠진 로마자 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-32.json` | 상태 변화 문항에 또 다른 가능한 상태 변화 답 `몸이 가볍게 됐어요` 포함 | 과거 상태 대조 `몸이 가벼웠어요`로 교체하고 설명 보강 | 복수 정답 드릴 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-33.json` | 해요체는 `never rude`라고 단정 | 일상 대화의 안전한 기본값이지만 표현·말투·격식 상황도 중요하다고 설명 | 사회언어학적 절대 규칙 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-33.json` | 반말 의문문은 억양만 쓰며 `-까?`가 없다고 단정 | 무표지 `가./가?` 쌍과 `-니?`, `-(으)ㄹ까?` 등 표지 의문형을 구분 | 반말 의문형 전체를 잘못 일반화한 설명 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-33.json` | `시작하겠습니다라고 말할 거예요` | `“시작하겠습니다”라고 말할 거예요` | 직접 인용 경계를 표시하지 않고 종결어미에 `라고`를 결합한 오류 교정 | 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-34.json` | `부모님` 주변 동사는 역할과 무관하게 모두 `계시다·드리다`를 써야 한다고 단정 | 주어 높임과 받는 사람 높임을 구분하고 문맥별 `주시다·드리다·있다·께` 선택 설명 | 주어와 수혜자 방향을 혼동한 높임법 설명 교정 | 부모 어휘 원본에 동일 오류 있음 |
| 2026-08-02 | `data/chapters/chapter-34.json` | `선생님께서 말씀하셨어요라고 말해요` | `“선생님께서 말씀하셨어요”라고 말해요` | 직접 인용 경계 없이 종결어미에 `라고`를 결합한 오류 교정 | 동일 오류 있음 |
