# 이관 진행 대장

각 지시 완료 시 해당 행을 갱신하고 같은 커밋에 포함할 것. 상태: ⬜ 대기 · 🔄 진행 · ✅ 완료 · ⛔ 차단

| # | 지시 | 상태 | 완료일 | 결과 수치 | 특이사항 |
|---|---|---|---|---|---|
| 01 | A2 반입·격차 리포트 | ✅ 완료 | 2026-08-01 | 챕터 23 / 한입 196 / hunt 31 · teach 28 | 원본 SHA-256 23/23 일치 · target null 73 |
| 02 | 추출기 확장 | ✅ 완료 | 2026-08-01 | A2 hunt 31/59 (52.5%) → 37/59 (62.7%) / 신규 테스트 21 | target null 73 → 36 · 정규 ㄷ 오탐 보정 · A1 기준선 무변경 |
| 03 | A2 감사·교정 | ✅ 완료 | 2026-08-02 | 발견 182 (blocker 35) / overrides +563 | audit 23/23 · 미수정 0 · guard 통과 · A1 올바른 변경 9곳 재스냅샷 |
| 04 | 레벨별 스플리팅 | ✅ 완료 | 2026-08-02 | entry 38kB / 최대청크 211kB | A1 기준선 무변경 · 34과 레벨 지연 로드 · 05와 통합 게이트 후 배포 ☑ |
| 05 | 단어장 전체 확장 | ✅ 완료 | 2026-08-02 | 단어 419 / 뉘앙스 419 / 짝 39 | 중복 통합 93 · WordbookTab 24kB · 실패 재시도 확인 · 배포 ☑ |
| 06 | B1 반입·감사 | ✅ 완료 | 2026-08-02 | B1 22챕터 / 242한입 / 741카드 · 발견 33 (blocker 17) | audit 22/22 · 미수정 0 · compiler 74 tests · A1 기준선 무변경 |
| 07 | B2·C1 반입·감사 | ✅ 완료 | 2026-08-02 | B2·C1 9챕터 / 92한입 / 301카드 · 발견 16 (blocker 10) | audit 9/9 · 단어 718 / 상세 718 / 짝 48 · 미수정 0 · A1 기준선 무변경 · 배포 ☑ |
| 08 | 책장 레벨 그룹 | ✅ 완료 | 2026-08-02 | 5그룹 / 65챕터 / 619한입 · 신규 테스트 5 | 기본 열림=다음 한입 레벨 · 검색 매칭 그룹 전개 · localStorage 복원 · A1 기준선 무변경 |
| 09 | 단어팩·리더 | ✅ 완료 | 2026-08-02 | 팩 12 / 리더 20 / 간식 12 / 멤버 173/173 | baseline 무변경 · ReadersShelf 31.22kB gz · snacks 28.82kB gz · 그릇·읽기 QA 통과 |
| 10 | 클러스터 브라우저 | ✅ 완료 | 2026-08-02 | 세트 32/32 / 멤버 89 | clusters 11.06kB gz 별도 청크 · 검색·오디오·뒤로 2단·기존 WordDetail QA 통과 |
| 11 | 홈 레벨 인지 | ✅ 완료 | 2026-08-02 | nextBite 5시나리오 / store 1테스트 | 레벨·간식 추천·세션 스킵·이어서·한 입 더 단일 소스 · 실제 1과 8입+간식 완주 QA |
| 12 | 최종 회귀·배포 | ✅ 완료 | 2026-08-02 | 총 챕터 65 / 학습 항목 631 / 카드 2,212 | guard:full 104테스트 · 감사 232/63/0 · 라이브 해시 `index-PzFaCE37.js` 일치 · A1·C1 QA · 배포 ☑ |

### 2단계 — 앱스러움

| # | 지시 | 상태 | 완료일 | 결과 수치 | 특이사항 |
|---|---|---|---|---|---|
| 13 | 설정 시트 + 로마자 정책 | ✅ 완료 | 2026-08-03 | entry 62kB / 신규 테스트 5 | 설정 6항목 · 로마자 즉시 표시·새로고침 유지 · 진행만 초기화 · 컴파일 산출 19파일 해시 불변 · A1 기준선 무변경 |
| 14 | 온보딩 + 시작점 선택 | ✅ 완료 | 2026-08-03 | entry 65kB / nextBite 테스트 7 | 3카드 · 시작점 1/2/12과 · 완전 처음 로마자 자동 · 진행 사용자 시작점 무시 · 컴파일 산출 19파일 해시 불변 · A1 기준선 무변경 |
| 15 | PWA화 | ✅ 완료 | 2026-08-03 | precache 14개 / A1 오프라인 ☑ / entry 67kB | SW runtime 3시나리오 · 컴파일 산출 19파일 해시 불변 · 배포 ☑ · 폰: iOS 공유→홈 화면에 추가 / Android 메뉴→앱 설치 |
| 16 | 소리·햅틱·피드백 | ✅ 완료 | 2026-08-03 | 피드백 4종 / 신규 테스트 2 / entry 68kB | guess·drill·order 재출제 안내 통일 · guess·payoff 자동 발음 · reveal 자동 스크롤 · 컴파일 산출 19파일 해시 불변 |
| 17 | 다크 모드 | ✅ 완료 | 2026-08-03 | 3계층 테마 / 토큰 외 하드코딩 색 0 / entry 69kB | OS 자동·밝게·어둡게 즉시 반영 · theme-color 동기화 · 쇼케이스 전환기 · 컴파일 산출 19파일 해시 불변 |
| 18 | 스트릭·수집·별표 | ✅ 완료 | 2026-08-03 | 문법 카드 236 / 신규 테스트 8 / entry 73kB | 스트릭·월~일 히트맵 · 기존 완료 문법 소급 · 별표 필터·단어장 상세 이동 · 컴파일 산출 19파일 해시 불변 |
| 19 | 진짜 SRS | ✅ 완료 | 2026-08-03 | srs 테스트 9 / entry 75kB / 재스냅샷 0회 확인 ☑ | 1·3·7·14·30·60일 간격 · due 6장부터 복습 한입 · 역방향 교대 · 책장 워밍업 제외 · SW 캐시 재점검 · 라이브 복습 entry 확인 · 배포 ☑ |

### 3단계 — 두께

측정 기준(2026-08-03, 두께 작업 전): 문법 바이트 문제 ≥2 비율 **0%**(헌트만 100 ·
티치만 89 · 헌트+드릴 47) · 본편 1장 바이트 **256/619** · 전체 중간값 **2장** ·
어순 이중 출제 **63건**.

| # | 지시 | 상태 | 완료일 | 결과 수치 | 특이사항 |
|---|---|---|---|---|---|
| 20 | 두께 게이트 | ✅ 완료 | 2026-08-03 | snapshot-all 65챕터/2,042카드 ☑ / density-before ☑ | additive FAIL 동작 확인 ☑ (삭제·수정 잡음, 추가 통과) · 카드키 공용 모듈화 · guard:full 그린(산출 무변경) |
| 21 | 문법 두께 | ✅ 완료 | 2026-08-03 | 문제 ≥2 비율 **0% → 100%** / 문제 0 바이트 **189 → 0** / 카드 +744 / 신규 테스트 10 | additive 위반 0 · 생성기 5종(클로즈 확장·타일·함정 픽·formTable·읽기 픽) · funcLead·pitfall 보존+렌더 · 런타임 문법 레슨 덱과 정합(타일 통과·함정 중복 제거) · interchangeableVariants 5쌍 · 레거시 안전 테스트 4건을 정제된 불변식으로 갱신 |
| 22 | 대화·읽기·중복 | ✅ 완료 | 2026-08-03 | 어순 중복 **63 → 0** / 대화 중간값 **2 → 4** / 읽기 중간값 **1 → 3** / 1장 바이트 **67 → 0** / 카드 +191 | 삭제 예외 등록 63건(전부 order) · 말한이+대화클로즈 · 지문회상 클로즈 2 · 전동사 어휘 챕터는 지문 어순 폴백(ch-08) · 신규 테스트 6 · 라이브 QA: 대화 CHAT→ORDER→WHO→CLOZE→WIN, 읽기 READ→RECALL×2→WIN |
| 23 | 워밍업·재감사·마감 | ✅ 완료 | 2026-08-07 | 중간값 **5** / 1장 바이트 **0** / 신규 blocker **0** (1,028장 전수 스크린) / 총 1,851kB gz | 워밍업 1·2장 비율(책장 0 유지) · 컴파일러 수정 3종(이형태 오답쌍·포함쌍 오답·타일 시작 힌트 153장) · b1a/b1b 분할 + 단어장 깊이 단일사본(총합 2,472→1,851kB, 예산 2.5MB 원상) · 재스냅샷 ☑ · guard/guard:full 그린 복귀 · 테스트 180 · 배포 ☑ |
| 24 | 문법 레퍼런스 뷰 | ✅ 완료 | 2026-08-07 | 시트 3경로 ☑ (그리드·미수집 배지·승리 화면) / 신규 테스트 2 / entry 84kB | 컴파일 무변경(baseline 그린) · 예문 조립은 컴파일 산출 내 재배열(grammarRef.js) — hunt 줄+클로즈 복원+타일 문장 중복 제거 · 재풀기=워밍업 없는 처음부터 · 미수집도 열람 가능(🔒 배지) · 배포 ☑ (24 완결 라이브 반영) |

### 4단계 — 확장 (스펙 2026-08-07 작성, 실행은 오퍼스/소넷)

| # | 지시 | 상태 | 완료일 | 결과 수치 | 특이사항 |
|---|---|---|---|---|---|
| 25 | 신규 챕터 반입 (66~72) | ✅ 완료 (☑ (감독자: SHA 7/7 재대조 · guard:full 재실행 그린 · A1 baseline 무변경 · check-additive 잔재는 신규 챕터와 무관함을 확인)) | 2026-08-08 | 반입 7개 / SHA 7/7 일치 / 신규 77한입·363카드 / blocker 0 | 부모 c10 출하분 전량 반입. 감사 기록 `../korean-core-starter/docs/superpowers/specs/v2-upgrade/audit/c10-chapter-{66..72}.json` — 전부 blocker 0(minor 5) · **부모 number 매핑**(반입은 파일 id 기준, 부모 number는 무시): 66→64, 67→65, 68→66, 69→67, 70→69, 71→70, 72→71 — 한입 64·65는 부모에서 63·72가 됐고 부모 number 68은 부모 chapter-63의 자리다 · levels.js 66+ 명시 테이블(66~69 B2·70~72 C1, 부모 `level` 필드와 대조 일치), 1~65 판정 무변경을 테스트로 고정 · 총계 72챕터/696한입/3,340카드, hunt 161·**teach 110**(신규 21은 피동·관용구 추출 한계로 폴백, 기록만) · **스크린 blocker 0**(minor 2) — 최초 2건은 70과 (으)나/(으)므로 이형태 오답쌍이었고, order 21 이전 spare 클로즈만 `interchangeableVariants`를 안 보던 누락을 메우고(테스트 선행) 금지쌍 2개 등록해 해소, **콘텐츠 무수정** · 청크: b2c1 자동 합류 203.5kB(분리 불필요), 대신 **grammarData 252kB 예산 초과 → early/late 분할**(87.9+164.5) · 단어장 720→**803**(신규 83·공유 49, 6개는 대표 항목이 새 챕터라 a1→b2/c1 샤드 이동) · 책장 헤더를 런 표기로 수정(`Chapters 57–63, 66–69`) · 테스트 199→**205** · entry 88kB·총합 2,103kB gz · **A1 baseline 무변경** · dev QA(:5180 재사용): B2/C1 그룹 노출·66과 문법 한입 완주(그릇 1·수집 2/271·문법 시트 ☑)·신규 표제어 밟히다 상세 로드 ☑ · 배포는 감독자 검증 후 · **관찰(범위 밖)**: `check-additive`가 stale exception 35건으로 빨갛다 — 지시 22가 등록한 삭제 예외를 지시 23 재스냅샷이 되살린 잔재로, 이번 반입과 무관(반입 전 상태에서도 동일) |
| 26 | 상황별 표현 팩 6개 | ✅ 완료 | 2026-08-07 | 팩 6 / 카드 84 / 중복 0 · 간식 12→18 (173→257장) / 신규 테스트 9 (182→191) / blocker 0 | 한국어 저작 0(부모 텍스트 재배열) · **소스 함정 2개**: `level` 전부 A1 + `learnerPriority`도 템플릿이라 순위 불가 → 부모 `sort`로 정렬(사유는 pack-selection-26.json에 기록) · 배치 실측(23·52·51·48·40·58과 뒤 — 지시서의 "63과(병원)"은 부모 번호, 한입 병원 챕터는 48) · 팩 2개 제목을 실측 내용에 맞춰 조정(식당→식당·카페, 관공서→은행·행정 — admin 상위 14개가 전부 은행) · reveal은 shortExplanation(889개 전부 "X is a practical expression meaning Y" 보일러플레이트) 대신 부모 nuance · 신규 팩 스크린 blocker 0(반입 단계에서 3건 교정: 두 문장 표제어 배제 1, 금지쌍 2) · **기존 단어팩 12개에서 선택지 1~2개뿐인 카드 33장 발견 — 범위 밖이라 기록만** (snack-cards.json) · snacks 청크 28.8→65.2kB gz(예산 220) · 총합 1,894kB gz · 65챕터·기존 12팩 카드 바이트 무변경 · 배포 ☑ (감독자 독립 검증: guard:full 재실행 그린 · 저작 0 — 표제어·예문·뉘앙스 504건 부모 원문 대조 일치 · dev QA 팩 노출/하이라이트/리빌) |
| 27 | 디자인 개편 (프로덕트 톤·영어 메인) | ✅ 완료 | 2026-08-08 | 리스타일 **31개** (스펙 프레임 12 + 카드 9 + 참고 탭·전역 10) / 신규 테스트 8 (191→199) / entry 85kB gz / **산출 17파일 해시 불변 ☑** (`cf9541ac`) | 확정 시안 홈 A·리빌 A · STYLE.md 신설 + tab-contract 언어 정책 교체(개정 이력 포함) · 메커니즘 3종 테스트 선행(SRS `nextIntervalDays`가 `applyRecord`의 유일한 사다리 · 오답 노트 `mistakes.js` · 홈 그릇 `bowlFill`) — 새 저장 필드 0 · **스펙과 다르게 판단 3건**: ①프레임 3(문제 상태)의 예문 영어 번역은 미노출 — 정답을 그대로 흘리는 카드가 다수라 추측 메커니즘이 무너진다(리빌에서 노출) ②플레이어 상단 카운트를 `{bite}/{chapter}입`→`{card}/{cards}`로 교체(스펙 표기이자 바로 옆 진행바와 일치) — 챕터 위치는 홈 `Bite N`이 담당, `biteNumber/biteTotal` prop 제거 ③프레임 8의 Check 버튼은 플레이어 CTA가 아니라 카드 안에 유지(카드↔플레이어 결합을 늘리지 않으려고 — 시각 비중은 CTA와 동일) · 라이브 QA에서 실버그 1건 발견·수정: 승리 화면 오답 노트의 긴 영어 뜻이 `flex:none`이라 가로 스크롤 455px 유발 → 줄바꿈 허용 · 컴파일러·데이터 파일 무수정(`grammarLesson.js`·`shelf.js`는 런타임 라벨) · 병기 도장 0(컴파일 산출 문자열 제외) · uppercase 라벨 0 · 이모지는 탭바 5개만 · guard:full 그린 · QA 8시나리오 ☑ (다크·라이트 양쪽) · 배포 ☑ (감독자: guard:full 재실행 그린·재컴파일 후 트리 클린=산출 불변·라이트 모드 온보딩/홈/문제/리빌 육안 재검) |

| 28 | 재질·구조 복원 (v5) | ✅ 완료 | 2026-08-08 | 결함 7 해소 **7/7** / 신규 테스트 **17** (205→222) / **산출 지문 불변 ☑** (`b9e75e57`) / entry 93kB gz · 총합 2,108kB gz | 확정 시안 홈 A · 한자 아이콘 文 · **그릇 사본 3개 → 공용 컴포넌트 1개**(`src/lib/bowl.js` 기하 + `src/components/Bowl.svelte` 렌더; 승리 112·온보딩 108·홈 86·주간 26·탭 23(line) — 전부 같은 패스, 작은 크기만 stroke 광학 보정) · 신규 로직 3종 **테스트 선행**(레드 확인 후 구현): 그릇 채움 분기(`bowl.js` — 0%는 도형 미출력, 100%는 고봉, `y=72-42·fill`이 스펙 프레임 8의 55.2/51/61.5/30과 전부 일치) · `findAfter`(승리 화면이 진행 중인 한입을 done으로 세고 다음 항목을 찾음, done 맵 무변이) · `chapterSealInfo`(챕터 도장 + 레벨 내 몇 번째인지) · **탭 이모지 0** — 5종 SVG 한 시스템(24×24·stroke 1.7·文만 1.55·currentColor), 앱 소스 픽토그램 이모지 0개(남은 `✓ ★ ☆ ✕`는 STYLE.md §4가 허용하는 본문 폰트 글리프) · 종이 결은 `<html>`에 칠해 전역화하고 카드 안쪽까지(`.tooth`) 이음 — `--study-grid` 토큰 값 무변경, 밀도만 2겹+3px 도트 · 덱 뒷장 -0.35°/+0.5° · **클래스 충돌 회피**: 플레이어 상단 `.bar`→`.playbar`, 진행 바는 `.progbar`, 책장 부속 행은 `.subrow`(리빌 `.sub`와 분리) · 홈 A의 「오늘 만날 말」은 `biteHeadwords`가 컴파일 산출에서 뽑고(뜻 미노출 — 추측 설계 보존) 그 로드가 Start용 청크 프리페치를 겸함, **새 필드·새 저장 0** · **스펙과 다르게 판단 4건**: ①탭 Today 아이콘은 채움 그릇이 아니라 같은 컴포넌트의 `variant="line"`(23px 채움 잉크는 나머지 4개와 광학 밀도가 안 맞고 스펙 프레임 7의 「하나의 그리기 시스템」이 깨진다) ②지시서의 132/44px 대신 실사용 크기 112/108/86/26/23 채택(132는 스펙 프레임 8의 전시용 치수, 44는 대응 화면 없음) ③홈 B(프레임 2·코스 척추)와 學 비교 컬럼은 참고용이라 미구현 ④「오늘 만날 말」은 항목이 2개 미만이면 섹션을 그리지 않는다(문법 한입은 무늬 이름 1개뿐 — 1개짜리 목록은 히어로와 중복) · 홈 세로 리듬을 스펙 값으로 조여 CTA가 첫 화면에 들어오게 함(15px 절감) · 컴파일러·데이터 파일 무수정, 재컴파일 후 트리 클린 · guard:full 그린(72챕터/696한입/3,340카드 · baseline A1 무변경 · 24파일 222테스트) · QA 8시나리오 ☑ (다크·라이트 양쪽) · **배포 ☑ (감독자: guard:full 재실행 그린 · 재컴파일 후 트리 클린=산출 불변 · Bowl clipPath 단일 확인 · 소스 이모지 0 · 라이트/다크 온보딩·홈 육안 + 넘침·CTA 겹침 0)** |

| 29 | 학생 프로필 + 진도 코드 이전 | ✅ 완료 | 2026-08-08 | 마이그레이션 손실 **0** (테스트 4종 + 라이브 실측) / 최악 코드 **3.65KB** (목표 3·상한 10) / 신규 테스트 **29** (222→251) / **산출 지문 불변 ☑** (`51a1a86f`) / entry 99kB gz · 총합 2,122kB gz | **마이그레이션이 최대 위험이라 테스트 선행**(레드 확인 후 구현): `migrateStorage`는 ①복사 ②네임스페이스 키를 다시 읽어 **바이트 동일 검증** ③그제서야 레거시 삭제 — 순서를 코드가 강제한다. 실패(파싱 불가·검증 실패)하면 **아무것도 만들지·복사하지·지우지 않고** 레거시 키를 그대로 남긴 채 보고하고, 앱은 `resolveKey`가 레거시 이름으로 폴백해 지시 29 이전 동작으로 degrade(빈 상태로 시작하는 경로가 존재하지 않음) · 테스트 4종 = ①레거시 이동 후 4키 바이트 동일+레거시 제거 ②빈 기기 신규 프로필(진도 키 미생성) ③재실행 무동작(전 키 값 동일) ④깨진 JSON 거부 + ④b 조용히 드롭하는 quota 기기에서 부분 복사본 롤백 · 라이브 실측(레거시 4키 심기 → 갱신): 진도 JSON 바이트 동일, prefs·srs는 스토어의 기존 normalize-on-load 재기록이라 **심은 값 전부 보존**(누락 0) · 저장 구조 `hanip.profiles-v1`(레지스트리)·`hanip.active-v1`(활성)·`hanip.p.<id>.{v1,prefs-v1,srs-v1,shelf-open-v1}` · 전환은 활성 기록 후 `location.reload()`(스토어가 모듈 초기화 시 1회만 읽으므로 부분 전환은 이전 학생 화면이 남을 수 있다 — 근거 주석 기재) · UI 3경로 = 온보딩 4단계(이름·건너뛰기 가능)·설정 시트 프로필 목록(전환·추가·이름변경·2단계 삭제)·홈 `이름 #번호` 11.5px ink-3(탭하면 설정) · **코드 크기**: 최악(learned 200·done 696·collected 271·srs 400·bowls 180) 원본 219KB → 소박한 deflate+b64 91.7KB(지시서 실측 75KB 재현) → **축약 14.9KB → 3.65KB**. 축약 3종 = learned를 ko 키만 싣고 가져오기 때 **컴파일 청크에서 카드 복원**(복원 실패는 개수로 보고) · done/collected를 `chapter-NN-bM` **id 문자열에서 파싱한** 챕터별 비트마스크로 접기(컴파일 산출의 배열 위치에 의존하지 않아 신규 챕터 반입 후에도 코드가 유효 — 실제 696 id 전수 왕복 테스트로 고정, 형식이 다른 id는 그대로 실림) · 완료 시각·bowls를 델타 인코딩(696개 절대 분값 2.2KB → 델타 0.17KB, 그래서 **왕복 무손실을 포기하지 않음**) · 가져오기는 병합 전용 — srs의 `reps`는 이 앱에 없고 `interval`이 고정 사다리[1,3,7,14,30,60]의 칸이자 정답으로만 올라가므로 reps의 정확한 대리값이라 판단해 **interval 큰 쪽 전체 채택·동률이면 due 이른 쪽** 적용(근거 주석 기재) · **원자성**: `planImport`가 전 필드 검증 후 다음 상태를 통째로 반환하고 호출자가 두 스토어를 커밋 — 중간 실패 시 무변경(테스트로 고정) · 미리보기 「3 new bites · 2 new words · 1 grammar card」 + 「Merged into 민수 from Learner #6136. Nothing already on this device is replaced.」 · **스펙과 다르게 판단 3건**: ①`prefs`/`shelf-open`은 병합 규칙상 항상 무시되므로 payload에 **null 자리만** 두고 실제 값을 싣지 않는다(쓰이지 않을 바이트가 3KB 예산을 먹는다 — 필드는 남겨 향후 확장 여지 유지) ②`lastPlayed`는 로컬 우선이되 로컬이 비어 있으면 가져온 쪽을 채택(기기 것 유지 원칙은 지키면서 새 기기의 Continue가 죽지 않는다) ③프로필 `lastSeenAt`은 생성 시 0(“Not started yet”) — 방금 추가한 학생이 “오늘 학습”으로 보이는 건 거짓이라, 완료된 한입(`markBiteDone`)과 가져오기 시점에만 **단조 증가**로 갱신 · 이름은 로컬 전용 — 전송 코드 0 · QR 미구현(지시서대로, 파일 저장이 대체) · 전송·압축 코드는 설정 시트에서 **지연 로드**(TransferSheet 6.7kB 별도 청크) · 컴파일러·데이터 파일 무수정, 재컴파일 후 산출 지문 동일 · guard:full 그린(72챕터/696한입/3,340카드 · baseline A1 무변경 · 26파일 251테스트) · **QA 7시나리오 ☑** ①레거시 심기→갱신: 진도·prefs·srs·shelf 그대로, 홈 Chapter 1 Bite 3·3-day streak·Continue 복원 ②프로필 2개 전환: 새 프로필은 온보딩부터, 키·진도 완전 분리 ③A 코드→B 붙여넣기: 미리보기 3/2/1 → 적용 후 done 합집합·bowls 08-07은 큰 값 5 유지·starred 합집합·srs 한글 interval 7 채택, **A 프로필 무변경** ④파일 저장·불러오기: `hanip-Learner-6136-2026-08-08.txt` 로드 → 자동 검사 → 병합 ⑤깨진 코드: 「This code is incomplete or damaged.」 거부, done 3건 그대로 ⑥삭제 2단계: 경고 「이 학생의 진도가 지워집니다」 → 확정 시 4개 키 전부 제거, 활성 프로필은 삭제 버튼 없음+전환 안내 ⑦다크·라이트 양쪽 렌더 ☑ (가로 넘침 0·콘솔 에러 0) · **배포 ☑ (감독자 독립 재현: 레거시 4키 심고 갱신 → 프로필 자동 생성·값 전부 보존·레거시 제거 확인, 홈에 Learner #0113·진도·그릇·다크 복원 / 코드 왕복 실측 250자 HANIP1. — done·starred·learned·srs 복원 / guard:full 재실행 그린 · 재컴파일 후 트리 클린)** |

| 30 | 단어 첫 만남은 설명 | ✅ 완료 | 2026-08-08 | 모드 분기 테스트 **18** (251→269) / 밀도 무회귀 ☑ / **산출 지문 불변 ☑** (`325a9c30`) / entry 100kB gz · 총합 2,123kB gz | 사용자 판정: 문맥 추론은 주변 어휘를 아는 B1 이후에나 성립 → 첫 만남 teach(단어→뜻→예문), 복습 quiz(현행) · **모드 판정은 순수 함수 한 개**: `srs.js`의 `guessMode(card, schedule)` — `warmup|review|confirm` 플래그면 무조건 quiz, 아니면 「그 단어가 일정에 있는가」만 본다(guess가 아닌 카드는 항상 quiz라 호출부에 분기가 없다). 플레이어가 한 번 호출해 `curMode`로 들고 렌더·채점·일정에 같은 값을 흘린다 · **일정 스냅샷 고정**: 한입을 여는 순간의 `get(srs)`를 얼려 쓴다 — 라이브로 읽으면 확인 문제가 기록한 직후 같은 런의 뒷카드가 teach→quiz로 조용히 바뀐다 · **teach 레이아웃은 새 화면이 아니라 리빌의 재배치**: `.answer` 블록(58px 히어로·AudioDot·별표·gloss·romaja·pos·`.note` 뉘앙스·wordbook 링크)을 그대로 앞으로 당기고, 예문만 `.ex` 절로 새로 끼웠다 — 크기도 기존 자산 재사용(`.ex-ko` 20px/700 = `.stem.small`의 예문 대역, `.ex-en` 11.5px = `.stem-en`, `.ex-head` 12.5px+11.5px 블록 = `.srs` 패턴). 목표어 gold 밑줄(`.tgt`)은 유지해 히어로와 예문의 같은 말이 이어져 보인다 · 새 타입 크기 0 · 리터럴 색 0 · quiz 분기는 `{#if !teach}`로 감싸기만 해 마크업 무변경 · **진행 버튼은 기존 하단 CTA**: teach 카드가 마운트에서 `onResolve(true,{teach:true})`를 던져 Next가 즉시 살아난다(카드 안에 두 번째 CTA를 만들면 화면당 CTA 1개 규율이 깨진다) · **오염 방지 3중 게이트, 전부 테스트된 순수 함수**: ①`scoresAnswer(card,mode)`가 false면 `resolve()`가 즉시 반환 — 점수·소리·햅틱·재출제·오답 노트 전부 못 지나간다 ②`writesSchedule(card,mode,correct)`가 SRS 기록의 유일한 관문(기존 「requeued+정답은 기록 안 함」 규칙도 여기로 흡수) ③`collectMistake`가 mode를 받아 teach면 자체적으로 거른다(기본값 quiz라 기존 호출부 무변경). **라이브 실측**: 새 챕터 단어 한입에서 teach 6장 통과 후 `srs === {}`, 승리 화면 점수 「2 of 3 on your own」(확인 2 + 재출제 1) · Mistakes 1건(틀린 확인 문제 1개뿐, teach 0) · **확인 문제(설계 B)는 `src/lib/confirm.js` 파일 하나로 격리**: 그 한입이 teach로 띄운 단어를 앞에서부터 최대 2개(가장 오래 지난 것부터) 골라 `{...card, confirm:true}`로 payoff **앞**에 끼운다 — 새 단어 1개면 1장, 0개면 배열을 그대로 반환. 제거는 파일+테스트 삭제 후 BitePlayer의 import 1줄·호출 2곳만 지우면 되고 teach/quiz 분기는 그대로 산다(파일 상단에 제거 절차 주석) · **스펙과 다르게 판단 2건**: ①지시서는 「문장이 없는 카드(target null 계열)는 예문 단락을 생략」이라 했으나 실측 결과 문장 없는 guess 카드는 **0개**이고 target null 34장은 전부 문장을 갖고 있다(`시간이 있다` ↔ 「내일 시간이 있어요?」) — 문구의 근거(문장 부재)가 성립하지 않고 규칙대로 지우면 34개 첫 만남이 사용자가 요구한 예문을 잃으므로, **문장이 있으면 예문을 보이고 목표어가 문장에 문자 그대로 있을 때만 밑줄**을 긋는다(생략 분기는 방어용으로 남김) ②로마자는 「히어로+로마자」 지시 대신 지시 13의 로마자 정책(설정 토글/`Show romanization` 버튼)을 그대로 재사용 — 정책을 카드 종류마다 갈라놓지 않는다 · **밀도 무회귀**: 컴파일 산출을 한 바이트도 건드리지 않았고(`bites.json`이 HEAD와 동일) 리포트는 그 파일만 읽으므로 수치가 움직일 수 없다 — 문법 문제 ≥2 **97.4%** · 문제 0개 **0개** · 어순 중복 **0건** · 전체 중간값 **4장** · 본편 1장 바이트 **1개**(`chapter-67-b10` 읽기 — 지시 25의 신규 챕터 66~72분으로 지시 23 스냅샷 이후 생긴 기존 상태, 지시 30 무관) · guard:full 그린(72챕터/696한입/3,340카드 · baseline A1 무변경 · 27파일 269테스트) · 테스트 선행(레드 10건 확인 후 구현) · **QA 6시나리오 ☑** ①새 챕터 단어 한입 1/9: 한글(58px)→Hangul→hangeul/noun→「Used like this·이렇게 써요」→한글은 배우기 쉬워요.(밑줄)→영역 번역→뉘앙스, 선택지·몰라요 없음·Next 활성 ②같은 바이트 재생: 7장 전부 quiz, **확인 문제 0장**(새 단어 없음) ③다음 바이트 워밍업 1/13 quiz ④복습 한입 7장 전부 quiz·방향 교대 유지(2번째가 en→ko) ⑤확인 문제 2장이 payoff 앞, 정답→`한글 interval 1`+「Next review tomorrow」, 오답→재출제 배너·오답 노트 1건 ⑥다크·라이트 양쪽 ☑(가로 넘침 0·콘솔 에러 0) · **푸시·배포 안 함** — 배포 ☑ (감독자 독립 재현: 새 학습자 1과 → teach 6장 연속 후 srs 비어 있음(오염 0) → 확인 문제 2장 정답 시 그 2단어만 interval 1 등록 · 단어→뜻→「이렇게 써요」 예문 순서 확인 · guard:full 재실행 그린 · 재컴파일 후 트리 클린) |

| 31 | 목차 표면 (Shelf 재설계·종이 규칙) | ✅ 완료 | 2026-08-08 | 결함 6 해소 **6/6** / 한 화면 챕터 **2 → 5개**(닿는 것 3 → 6) / 신규 테스트 **14** (269→283) / **산출 지문 불변 ☑** (`51a1a86f`) / entry 103kB gz · 총합 2126kB gz | 확정 **B안(그룹형) + A안 두 결정 흡수** · **종이 3층 규칙을 앱 전역 규칙으로 명문화**(STYLE.md §10-1·§10-2 신설, tab-contract 개정 이력 31행): 학습=괘선 2겹+도트(현행 `html`) / 목차=`.index-surface`(도트만, `--study-grid` 그대로 레이어 5→1) / 시스템=평면. **새 토큰 0** · 적용 표면 6곳 — Shelf(래퍼 1개가 GrammarCollection·ReadersShelf·검색 결과까지 덮는다) · WordbookTab(목록·헷갈리는 짝) · 가이드 인덱스. **가이드는 한 탭 안에서 층이 갈려서**(인덱스=목차, 본문=학습) GuideTab이 `onSurfaceChange`로 바깥 컨테이너에 층을 알리고 App의 `.guide-wrap`이 전체 폭으로 풀렸다 — 바깥에서 내부 상태를 추측하지 않는다. WordDetail·ReaderView·가이드 본문·홈은 1층 유지 · **거터 `[번호 24px][상태 22px]`**, 오른쪽 끝은 비움(chevron은 레벨 헤더 5개에만) · 상태 3종은 `StatusCell.svelte` 하나(빈 링 → gold 호 → 22px 한입 도장; 면적·열 동일, 잉크량만 단조 증가), 단어장은 같은 셀에 `✓/★/☆`(★가 ✓보다 우선 — 12 saved vs 214 learned) · **레벨 진행을 bite→챕터 단위로 전환**(`buildShelfGroups`의 `done/total`이 이제 챕터 수; 간식은 목록에만 남고 진행에서 빠진다 — 추천이지 코스 위치가 아니다) — 기존 테스트 2건을 새 의미로 갱신하고 「한입 3/4인데 챕터는 1」을 고정하는 테스트를 추가 · **테스트 선행**(레드 15건 확인 후 구현): `chapterProgress`(3상태) · `currentChapterId`(72행 중 1행) · `buildCourseRail`(레벨별 눈금·now 1개·빈 레벨 제외·완주 시 now 0) · `buildSnackBlock`(접힘 요약/펼침 항목/챕터별 독립) · `chapterRangeLabel` 무회귀 · **스펙과 다르게 판단 3건**: ①간식 펼침에 **접기 경로가 필요**해 「Snacks N」 디스클로저 줄을 펼친 뒤에도 남겼다(스펙 프레임 ③은 펼치면 라벨이 완전히 사라져 되돌릴 방법이 없고, `aria-expanded` 트리거도 사라진다) — 결함 2의 실제 기전인 **구분선 제거 + 54px 들여쓰기**는 그대로고 개별 행에는 라벨이 없다 ②미니 내비를 3열이 아닌 **2열(Grammar·Guides)**로 압축 — 지시서가 말한 「두 진입점」이 그 둘이고, Readers는 지연 로드되는 하단 섹션이라 카운트 버튼을 만들면 로드 전에는 갈 곳 없는 링크가 된다(스펙 프레임 ④도 Readers를 하단 섹션으로 그린다) ③레벨 접힘 줄의 「In progress」는 챕터 제목을 `·` 앞에서 자르지 않고 전체를 넣고 ellipsis 처리(스펙의 `split(" · ")[0]`은 시안 편의) ④**코스 레일의 눈금 pitch를 고정 2.6px에서 분수 트랙으로 교체** — 스펙 값대로 깔면 72눈금이 336.6px이라 **360px 폭 기기에서 가로 스크롤이 난다**(320px에서 실측 31px 넘침 확인). 레벨별 `repeat(n, minmax(0,1fr))` 트랙 + 레벨 사이 1.8px 스페이서(좌우 gap 1.8과 합쳐 스펙의 5.4px 간격)로 바꿔 어느 폭에서도 칼럼을 정확히 채우게 했다(320px에서 1.86px · 390px에서 2.83px · 480px 칼럼에서 4.08px, 넘침 0). 라벨 줄도 같은 트랙 목록을 써서 폭이 변해도 런과 어긋나지 않는다(실측: 런 시작점과 라벨 좌표 5/5 일치) · 한입 행도 부속이라 구분선을 걷고 54px로 들여썼다(kind 라벨은 유지 — 간식과 달리 종류가 실제 구분이다) · **실측(370×762, 스펙 프레임 ①과 동일 기준)**: 헤더 383→**285px**, 챕터 행 86→84px(진행 중 1행만 119px), 온전히 보이는 챕터 **2→5**(간식 1챕터 펼친 상태에선 3, 스펙 프레임 ③의 3과 일치) · 컴파일러·데이터 파일 무수정, 재컴파일 후 산출 지문 동일 · guard:full 그린(72챕터/696한입/3,340카드 · baseline A1 무변경 · 27파일 283테스트 · entry 103kB) · **QA 7시나리오 ☑**(별도 origin `127.0.0.1:5180`에서 검증 후 원상 복구 — 사용자 프로필 미접촉) ①A1 펼침: 간식 전부 접힘 한 줄 ②2과 간식 펼침: 개별 3행, 구분선 0·라벨 0·54px 정렬 ③목표+분수는 3과 한 행에만(1·2과 도장, 4~11과 빈 링) ④레벨 5개 접힘+코스 레일: 72눈금·gold 2·잉크 1(6px 김)·「Chapter 3 of 72 · 2 done」·현재 레벨만 In progress ⑤단어장(무지+왼쪽 ✓/☆ 열, ✓12·☆791)·문법 수집(무지 위 수집 카드만 tooth) ⑥검색 `38`: B1 1행, 2줄 제목 클램프 ⑦다크·라이트 양쪽 ☑ + 1280px 폭에서 좌우 여백 괘선 0 · **가로 넘침 0**(320·370·390·1280 전부 실측) · 새 탭 콘솔 에러 0 · **관찰(범위 밖)**: `.claude/worktrees/gracious-nash-eb88f6`에 저장소 사본이 남아 있어 vitest가 53파일/534테스트로 집계된다(전부 그린). 위 27/283은 사본을 제외한 실제 수치 · **푸시·배포 안 함** — 배포 ☑ (감독자 재현: A1 펼침에서 챕터 5개 노출·간식 한 줄 접힘·거터 번호+상태·괘선 0 육안 확인 · 320px 가로 넘침 0(코스 레일 분수 트랙 수정 유효) · guard:full 재실행 그린 · 재컴파일 후 트리 클린) |

| 32 | 결함 카드 + 신규 챕터 두께 | ✅ 완료 (감독자 검증 대기) | 2026-08-09 | 결함 **33 → 0** / 문법 ≥2 **97.4% → 100%** / 1장 바이트 **1 → 0** / 신규 blocker **0** / 신규 테스트 **12** (283→295) / 카드 3,340 → **3,348** / entry 103kB gz · 총합 2,127kB gz | **테스트 선행**(레드 9건 확인 후 구현) · ①**오답 생성 티어화**: `sharesContentWord`는 글로스 전체를 한 문자열로 읽어서, 「one (native number)」처럼 **분류 주석**을 공유하는 팩은 후보 풀이 전멸했다(숫자 23장 선택지 1개, 맞장구 9장 2개, 총 33장). 고친 방식은 규칙 완화가 아니라 **최후 수단 티어** — 티어 0은 출하된 규칙 그대로라 이미 오답 2개를 채우던 카드는 **바이트 동일**로 컴파일되고(챕터 단어 카드 1,188장 전수 무변경으로 증명), 굶은 카드에만 티어 1(주석·용법 노트를 뺀 **뜻**만 비교: `glossSense`가 괄호와 em 대시 뒤를 걷어낸다)과 티어 2(**팩 전체가 공유하는 낱말**은 팩을 분류할 뿐 카드를 가르지 못하므로 무시 — `packClassifiers`, 40%/최소 3장 기준)를 얹는다. **어느 티어도 동의 오답을 못 들인다**: 1차 뜻이 같으면(하나 "one" ↔ 일 "one", 열 ↔ 십) 전 티어에서 거부, 감사 금지쌍도 전 티어에서 거부 · ②**팩 한정 포함 가드**: 오답의 표제어가 정답 안에 들어앉으면(방 ⊂ 가방) 부분 읽기라 공정한 답이 된다 — 지시 26이 팩 스크린에 넣어둔 규칙을 생성기에도 넣었다. **팩에만 적용**(`compileSnack`이 `{pack:true}`) — 챕터에 걸면 23·34·40과의 기존 카드 5장이 바뀌어 게이트를 어긴다(관찰 결과 아래) · ③**신규 챕터 생성기 2종**(둘 다 `questionCount() < 2`일 때만 발동 — 이 게이트가 기존 65과 무변경을 기계적으로 보장한다): **⑥ formTable 표면 클로즈** — 관용구·속담·파생 접미사·문어 연결어미는 **제목에서 형태소가 안 나오지만** 표가 형태를 문자로 적어 둔다. `tableSurfaces`가 `add`열과 `ex`열의 화살표 우변에서 한글 전용 표면을 뽑고, **그 표면이 예문에 문자 그대로 나타나는 곳**을 파서 같은 표의 다른 표면을 오답으로 세운다(자동화/고령화/국제화, 티끌 모아 태산/우물 안 개구리/하늘의 별 따기, 있음/없음/바람). 오답 조건 = 문장에 없음 · 포함·금지쌍 아님 · **앞 2음절 공유 금지**(손이 크신↔손이 커요는 같은 형태 두 번) · 띄어쓰기 계층 일치 · 길이 2배 이내. **⑦ 넓힌 재조립** — B2/C1 예문은 7~9어절이라 6어절 타일 상한에 걸려 있었다. 상한을 8로 넓히되 **문제 2개에 못 미친 노트에서만** · ④**쉼표 목록은 타일 금지**: 「-대요, -래요, -냬요, -재요로」는 순서가 유일하지 않아 정답을 오답 처리한다 — 어절 2개 이상이 쉼표로 끝나면 타일을 만들지 않는다(전 경로 적용, 기존 533장 중 해당 0장이라 무변경) · 67과 읽기는 **지문 폴백의 2차 패스**(6어절 → 없으면 8어절)로 해결, 1차 패스는 손대지 않아 기존 읽기 바이트가 줄을 바꾸지 않는다 · **새 한국어 저작 0** — 모든 신규 카드의 모든 문자열이 노트가 이미 쓴 표·예문·함정 텍스트 · **기존 65과 카드 무변경 증명**: 전 72챕터 696바이트를 `cardKey`로 스냅샷해 전후 대조 → 1~65과 **변경 바이트 0**, 66~72과만 8바이트(`67-b10` 1→2, `68-b4`·`70-b8`·`71-b2`·`71-b6`·`72-b2`·`72-b4`·`72-b8` 각 2→3). check-additive도 **삭제·수정 0 / 추가 8** · **간식 카드 변경 33장 = 결함 33장과 정확히 일치**(부수적으로 바뀐 정상 카드 **0장**) — 숫자 23장 1→3, 맞장구 9장(2→3, 다행이네요는 1→3), 가방 1장(room→door) · **베이스라인 차이**(재스냅샷은 감독자 몫, `--write` 미실행): `baseline-a1.json` **차이 없음**(A1 무변경) · `snapshot-all.json`은 위 8카드만큼 뒤처짐 · `src/lib/snacks.json`은 스냅샷 대상이 아니라 커밋된 산출 파일 자체가 33장 변경분을 담는다 · **guard:thin 그린**(compile+validate+additive+546테스트) · **guard:full도 그린**(지시서는 레드를 예상했으나 baseline은 A1만 보고 A1이 무변경이라 통과) · `audit-generated` 신규 8장 **blocker 0·minor 0**, 팩 스크린 **preexisting 0** · **임의 판단 3건**: ①`audit-generated`가 덮어쓴 `phase3-generated.json`을 HEAD로 되돌렸다 — 스코프 문자열이 「orders 21-22」로 박혀 있어 이번 8장으로 갈아치우면 지시 25의 363장 기록이 거짓 라벨과 함께 사라진다(감사 결과는 이 행에 기록) ②티어 완화와 포함 가드를 **팩에만** 적용 — 게이트의 「신규 챕터와 간식 팩만 변경 허용」이 챕터 완화를 금지한다 ③`.claude/launch.json`(hanip-dev :5180)을 만들었으나 **커밋하지 않음**(로컬 도구, `.claude/`는 추적 대상이 아님) · **QA ☑**(신규 프로필 Learner #3873 — 사용자 프로필 미접촉): 숫자 팩 하나·둘·셋·넷 **선택지 3개** ☑ · 맞장구 맞아요 3개(긴 글로스 2줄 줄바꿈, 넘침 0) ☑ · 68과 V-는 바람에 7타일 재조립(시작 힌트 ☑) · 71과 -화(化) 「생산 공정이 ___되면서」 자동화/고령화/국제화 ☑ · 72과 속담 「___이라고, 매달 조금씩」 티끌 모아 태산/우물 안 개구리/하늘의 별 따기 ☑ · 67과 읽기 2장(read → 「네 가지는 원래 문장의 종류를 그대로 따라간다.」 재조립) ☑ · **관찰(범위 밖)** ①챕터 오답 중 **표제어 포함쌍 7장**(23과 타다↔갈아타다 2 · 34과 께↔께서 2 · 40과 겠↔좋겠어요 1 · 69과 버리다⊂잃어버리다 · 72과 발⊂마당발) — 팩과 달리 챕터에는 이 스크린이 없다. 1~65과 5장은 이번 게이트가 변경을 금지하므로 손대지 않음 ②`.claude/worktrees/gracious-nash-eb88f6`은 **git에 등록된 살아 있는 워크트리**(branch `claude/gracious-nash-eb88f6` @ 27a62dd, 트리 클린, `worktree prune` 대상 아님)라 지시대로 **건드리지 않았다** — 고아 디렉터리는 0개. vitest 이중 수집(26파일)은 그대로이며(546 vs 실제 295) 해소하려면 감독자가 워크트리를 제거하거나 vitest exclude를 정하는 판단이 필요 · **푸시·배포 안 함** — 재스냅샷·배포 감독자 대기 |

| 33 | 책장 초점 (B안+레벨 스트립) | ✅ 완료 (감독자 검증 대기) | 2026-08-09 | 진단 **3/3 해소** / 헤더 **4겹 → 2겹**(제목 줄·레벨 스트립) / 목록 시작 y **223.2 → 118.0px**(−105.2, 스펙 ⑤ 예측 −104.4) / 첫 챕터 y **355.0 → 249.8px** / 한 화면 온전한 챕터 **5 → 5(그중 1개가 카드)** / 신규 테스트 **22**·삭제 5 (shelf.test.js 25 → 42, 저장소 295 → **312**) / **산출 지문 불변 ☑** (`8da3db88`) / entry 105kB gz · 총합 2,129kB gz / 넘침 **0** @ 320·375·390·1280 | 실측은 전부 390×763 라이브(가시 영역 702px), before/after 같은 학습자(1~13과 완료·14과 4/9)로 대조 — before는 지시 31 커밋을 stash로 되돌려 같은 브라우저에서 잰 값이다(스펙의 218.4/349.2와 4.8px 차이는 실제 폰트 렌더 차) · **레일 제거**: `buildCourseRail` 삭제, 남은 세 숫자만 `shelfPosition`으로 이관 — 72눈금(눈금 폭 2.8px)이 답하던 「어디쯤」은 제목 줄 한 줄 + 레벨 헤더의 22px 상태 셀이 나눠 가진다 · **레벨 스트립**은 pill 금지(STYLE §4-4) → §7 밑줄 탭. 5셀 `A1 11/11` + 3px 진행 밑줄, 각 44px, 탭하면 점프·펼침, **자기 레벨을 탭하면 카드로** 복귀(부유 「현재 챕터로」 버튼을 만들지 않은 대가를 이걸로 갚는다). 스티키 금지 — 붙이면 「항상 떠 있는 A1~C1」이 부활 · 트랙은 `grid-auto-columns:minmax(0,1fr)` 분수. 320px에서 셀 52px·라벨 52px로 딱 맞아 `@media(max-width:359px)`에서 12.5→11.5px·10.5→10px로 반 포인트 내림(지시 31이 고정폭 레일로 320px를 31px 넘긴 전례를 피함) · **강조 카드**는 목록 안 제자리, 좌우 −10px 내밀고 패딩 10px → 카드 안 거터가 목록 거터와 같은 x(실측 카드 left 12 / 내용 22 = 목록 행 left 22) · 카드 대상 **3분기**를 `shelfFocusChapter`가 고정: 진행 중 → 없으면 다음에 시작할 → 완주면 null. **진행 중이 코스 순서를 이긴다** — 지시 31의 `currentChapterId`(첫 미완료 챕터)는 건너뛴 학습자에게 「손대지 않은 14과」를 현재라 부르므로 **삭제**했다(「현재 챕터」 정의가 둘이면 헤더와 카드가 다른 과를 가리킨다). 라이브 검증: 1~13 완료 + 56과 3/12 상태에서 카드=56과, 헤더=`Chapter 56 of 72`, 기본 펼침=B1, 홈은 같은 순간 14과 Bite 1을 고름 · **CTA 목적지**는 그 챕터의 다음 미완료 한입 — 14과 4/9에서 「단어 Words 3 · 알게 되다~」(5번째 한입) 열림 확인 · **검색**은 44px 아이콘으로 접되 `filterShelfGroups` 그대로(「기로」 → 14·32·54·60과 3레벨). 닫을 때 질의도 비운다(`toggleShelfSearch`) — 닫힌 필드 뒤의 필터는 사라진 챕터를 설명할 것이 화면에 없다 · **옆문 3개**를 하단 「More」 한 블록으로 통합(Grammar·Guides·Readers). Readers는 인라인 목록에서 행+전용 화면(← Shelf)으로 바뀌었고 lazy 청크는 그대로(ReadersShelf 30.8kB gz, entry 불변) · **스펙과 다르게 판단 2건**: ①**진입 스크롤을 조건부로** 했다 — 카드가 뷰포트(탭바 78px 제외)에 80% 이상 보이면 움직이지 않는다. 무조건 가운데 정렬은 14과(y=356)에서도 제목 줄과 스트립을 매 방문 위로 밀어내 스트립이 「도착했을 때 쓰는 컨트롤」이 되지 못하고 진단 1이 이름만 바꿔 되살아난다. 판정은 `cardNeedsScroll(box, viewportH)` 순수 함수로 빼고 6테스트로 고정(80% 경계 ±1px 포함). 56과(y=1400)는 그대로 스크롤됨을 라이브 확인 · ②**옆문에서 돌아올 때도 카드로 복귀**시켰다(스펙엔 없음) — Readers/Grammar는 Shelf 내부 상태라 `onMount`가 다시 돌지 않아 목록 맨 위로 떨어졌다 · **완료 기준 미달 1건**: 「한 화면 챕터 ≥6」은 **5**다. 지시서의 6은 프레임 ③(순수 B안, 스트립 없음) 수치이고, 확정안 ⑤는 스펙 본문이 이미 「온전한 챕터 5개(그중 1개가 카드)」로 실측·명시했다(스트립 44px이 챕터 한 행을 먹는 것을 스펙이 「남는 비용 ①」로 기재). 확정을 따르고 수치를 숨기지 않는 쪽을 택했다 — 6으로 올리려면 스트립을 빼야 하고 그러면 진단 1이 미해결로 돌아간다 · QA는 별도 프로필 `QA 33 #9933`로만 진행하고 뒷정리, 기존 프로필 키는 읽지도 쓰지도 않음 · 배포 □ 감독자 몫 |
| 34 | 카드 여백 + A1 지시문 언어 | ✅ 완료 (감독자 검증 대기) | 2026-08-22 | 카드 위 여백 **210 → 11px** / 답변 후 CTA 겹침 **30 → 0px** / 빈 공간 **43% → 38%** / 한국어 단독 UI 문자열 **3 → 0** / 신규 테스트 **10** (312 → **322**) / **산출 지문 불변 ☑**(재컴파일 후 트리 클린) / entry 105kB gz(불변) · 총합 2,129kB gz | 실측은 전부 375×812 라이브. **테스트 선행**(레드 확인 후 구현) · **A. 재생 화면을 한 뷰포트로**: `.deck`의 `margin: auto 0`가 카드를 세로 중앙에 매달아 짧은 카드는 위 210·아래 138px 허공에 뜨고, 긴 카드(리빌 691px)는 sticky CTA 밑으로 들어가 자기 마지막 줄인 `See this word in the wordbook →`(bottom 716)를 CTA(top 709)로 **30px 덮었다**. 새 `.stage`가 playbar와 CTA 사이에서 스크롤을 소유하고 CTA는 스크롤 밖으로 나가 **겹침이 구조적으로 불가능**해진다 — 리빌 상태 재측정에서 링크 bottom 700 < nav top 701 ☑. `.bp.playing`에만 `height:100dvh`를 걸어 **승리 화면은 계속 자란다**(무조건 고정하면 오답 목록이 긴 판에서 잘린다) · **B. A1 지시문은 영어가 이끈다**: 새 `src/lib/instructions.js`(`biteLevel`/`instructionLead`). 판정을 **컴파일이 아니라 런타임**에 둔 이유 — 레벨은 학습자의 위치이지 카드의 성질이 아니고, 컴파일러에 넣으면 UI 결정이 얼어붙은 `bites.json`에 들어간다. `BitePlayer`가 바이트를 열 때 한 번 계산해 Guess·Hunt·Order 3종에 넘긴다(워밍업이 다른 챕터에서 끌려와도 **학습자가 연 바이트**를 따라간다). `.ask-en`에 `keep-all` 추가 — 이제 한국어를 담을 수 있다 · **C. 계약 위반 3건 해소**: 복습 안내(`BitePlayer` 「틀렸던 카드는…」)와 그릇 안내 3분기(`Home.bowlHint`)가 한국어 단독이라 대상 학습자가 못 읽었다. 규칙 3대로 영어 메인 + 11.5px 한국어 보조 · **계약 개정 1건**: `tab-contract.md` 언어 정책 4항을 레벨 조건부로(1·2·3·5항 무변경, 개정 이력에 기록) · **스펙과 다르게 판단 1건**: 카드가 스테이지를 **꽉 채우게**(`flex:1`) 하면 빈 공간이 0이 되지만 시안을 띄워 비교한 결과 텅 빈 긴 종이가 되고 뒷장 스택이 가려져 카드로 안 읽힌다 — **내용 높이를 유지하고 위쪽에 붙이는** 쪽을 택했다 · **완료 기준 미달 1건**: 빈 공간 총량은 43% → **38%**에 그친다. 위 여백은 사라졌지만 남는 높이가 카드 **아래**로 옮겨간 것이라, 총량을 더 줄이려면 카드 내용이 커져야 한다 — 관찰 ①의 선택지 행 승격이 그 몫이고 이번 범위 밖이다 · **컴파일러·데이터 무수정** — `check-additive` 삭제·수정·추가 **0**, `baseline` A1 무변경, 재컴파일 후 트리 클린 · **관찰(범위 밖)** ①선택지 행 **46px**(44 최소 턱걸이)에 헤어라인뿐이라 버튼으로 안 읽히고, 성격이 반대인 `Don't know — just show me`가 **선택지 리스트 안 4번째 행**에 같은 시각 언어로 들어가 있다 ②승리 화면 `Next:` 우측의 고아 숫자(바이트 수로 보이나 라벨 없음) ③오답 시 진행 분모가 9 → 10으로 **증가** ④책장 챕터 2~5 제목이 `N은/는 · N이에요/예요` 같은 문법 표기 단독(챕터 1만 영어 병기) ⑤버튼에 `user-select:none`이 없고 `-webkit-tap-highlight-color`가 안드로이드 기본 파랑 그대로 · **푸시·배포 안 함** — 감독자 대기 |

## 기준선 재스냅샷 이력

A1 출력이 의도적으로 바뀌어 `baseline.mjs --write`를 실행한 경우만 기록.

| 날짜 | 지시 | 바뀐 것 | 사유 |
|---|---|---|---|
| (초기) | — | 최초 스냅샷 (89한입/334카드) | 하네스 구축 시점 |
| 2026-08-02 | 03 | A2 혼동쌍 금지가 A1 guess 9카드의 선택지를 교정 | 감사 판정 |
| 2026-08-02 | 후속 UI | A1 문법 제목 23곳 `무늬 Pattern` → `문법 Grammar` | 사용자 요청에 따른 학습 유형 명칭 정리 |
| 2026-08-08 | 32 | snapshot-all에 신규 8카드 반영(신규 챕터 66~72의 문법 문제 7 + 67과 읽기 1). **A1 baseline은 변화 없음** — 이번 변경은 66~72과와 간식 팩에만 닿았다 | 지시 32가 라이브 결함 카드 33장과 신규 챕터 두께를 고치며 의도적으로 산출을 바꿨고, 감독자가 기존 65과 무변경을 확인한 뒤 승인 |
| 2026-08-07 | 23 | 3단계 두께 작업으로 전 레벨 카드 2,042 → 2,977 (A1 포함 생성 문항·오답 위생·타일 힌트 반영) · snapshot-all 동시 갱신 | 지시 23이 승인한 단일 재스냅샷 — 이후 guard:full 그린 |
| 2026-08-08 | 25 | **snapshot-all만** 65챕터/2,977카드 → 72챕터/3,340카드 (신규 챕터 66~72 추가분) · A1 baseline-a1.json은 손대지 않음(무변경 확인) | 챕터 반입 — 기존 65챕터 카드는 스냅샷 대비 차이 0, 신규 7챕터만 추가 |
