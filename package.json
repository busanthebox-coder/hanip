{
  "name": "hanip",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run compile && vite",
    "build": "npm run compile && node scripts/validate-bites.mjs && vite build",
    "preview": "vite preview",
    "compile": "node scripts/compile-bites.mjs",
    "validate": "node scripts/validate-bites.mjs",
    "test": "vitest run",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "gh-pages": "^6.3.0",
    "svelte": "^4.2.19",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
